<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body style="font-size:18px; padding:40px; font-family:'Helvetica', 'Arial', sans-serif">
    <% include partials/_header %>
    <p><a href="<%= longURL %>"><%= longURL %></a></p>
    <p>Short URL: <%= shortURL %></p>
    <form method="POST" action=<%= `/urls/${shortURL}` %>>
      <label for="longURL">Enter the updated URL:</label>
      <input id="longURL" type="text" name="longURL" placeholder=<%= longURL %> style="width: 300px">
      <input type="submit" value="Update">
    </form>
</body>
</html>